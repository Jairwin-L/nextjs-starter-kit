enum UserStatusType {
  active
  pending
  restricted
  banned
  inactive
}

enum PermissionType {
  system
  page
  module
  operation
  data
}

enum RoleStatus {
  ENABLED
  DISABLED
}

enum AiProviderKind {
  OPENAI_COMPATIBLE
  ANTHROPIC
  GEMINI
}

enum ChatRole {
  SYSTEM
  USER
  ASSISTANT
  TOOL
}

enum ChatMessageStatus {
  STREAMING
  COMPLETED
  STOPPED
  ERROR
}
